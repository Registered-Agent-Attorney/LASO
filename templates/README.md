# Application templates

Start by copying a pipeline from `examples/`. The deterministic hello pipeline is
the smallest application template. Agent review adds offline model calls; human
approval adds a durable decision boundary. Register deployment-owned functions and
tools through application services before starting workers. Do not put deployment
logic or secrets into LASO core.

These are generic starting points, not preconfigured assistants.
