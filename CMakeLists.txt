cmake_minimum_required(VERSION 3.22)
project(LASO VERSION 0.1.0 LANGUAGES C CXX)
if(POLICY CMP0167)
  cmake_policy(SET CMP0167 NEW)
endif()
if(NOT CMAKE_SYSTEM_NAME STREQUAL "Linux")
  message(FATAL_ERROR "LASO targets Linux. Configure in a Linux build environment.")
endif()
include(CTest)
include(GNUInstallDirs)
option(LASO_ENABLE_ASAN "Enable AddressSanitizer" OFF)
option(LASO_ENABLE_UBSAN "Enable UndefinedBehaviorSanitizer" OFF)
option(LASO_ENABLE_TSAN "Enable ThreadSanitizer" OFF)
option(LASO_ENABLE_TIDY "Run clang-tidy during compilation" OFF)
option(LASO_ENABLE_POSTGRES "Build the optional PostgreSQL storage backend" OFF)
option(LASO_BUILD_OPENCODE_ADAPTER "Build the optional OpenCode worker adapter" OFF)
option(LASO_BUILD_CODEX_ADAPTER "Build the optional Codex worker adapter" OFF)
option(LASO_BUILD_CLAUDE_ADAPTER "Build the optional Claude Code worker adapter" OFF)
if(LASO_ENABLE_TSAN AND (LASO_ENABLE_ASAN OR LASO_ENABLE_UBSAN))
  message(FATAL_ERROR "Use TSan in a separate build")
endif()
find_package(Threads REQUIRED)
find_package(yaml-cpp REQUIRED)
find_package(nlohmann_json 3.10 REQUIRED)
find_package(SQLite3 REQUIRED)
find_package(spdlog REQUIRED)
find_package(CLI11 CONFIG REQUIRED)
find_package(Boost 1.74 REQUIRED COMPONENTS system)
if(LASO_ENABLE_POSTGRES)
  find_package(PkgConfig REQUIRED)
  pkg_check_modules(LIBPQXX REQUIRED IMPORTED_TARGET libpqxx)
  pkg_check_modules(LIBPQ REQUIRED IMPORTED_TARGET libpq)
endif()
include(FetchContent)
set(JSON_VALIDATOR_BUILD_TESTS OFF CACHE BOOL "" FORCE)
set(JSON_VALIDATOR_BUILD_EXAMPLES OFF CACHE BOOL "" FORCE)
set(JSON_VALIDATOR_INSTALL OFF CACHE BOOL "" FORCE)
FetchContent_Declare(json_schema_validator
  GIT_REPOSITORY https://github.com/pboettch/json-schema-validator.git
  GIT_TAG c780404a84dd9ba978ba26bc58d17cb43fa7bc80)
FetchContent_MakeAvailable(json_schema_validator)
if(TARGET yaml-cpp::yaml-cpp)
  set(LASO_YAML_TARGET yaml-cpp::yaml-cpp)
else()
  set(LASO_YAML_TARGET yaml-cpp)
endif()
add_library(laso_options INTERFACE)
set(LASO_SANITIZER_FLAGS "")
target_compile_features(laso_options INTERFACE cxx_std_20)
target_compile_options(laso_options INTERFACE -Wall -Wextra -Wpedantic)
foreach(sanitizer IN ITEMS ASAN UBSAN TSAN)
  if(LASO_ENABLE_${sanitizer})
    if(sanitizer STREQUAL "ASAN")
      set(flag address)
    elseif(sanitizer STREQUAL "UBSAN")
      set(flag undefined)
    else()
      set(flag thread)
    endif()
    target_compile_options(laso_options INTERFACE -fsanitize=${flag} -fno-omit-frame-pointer)
    target_link_options(laso_options INTERFACE -fsanitize=${flag})
    list(APPEND LASO_SANITIZER_FLAGS -fsanitize=${flag} -fno-omit-frame-pointer)
  endif()
endforeach()
if(LASO_ENABLE_TIDY)
  find_program(LASO_CLANG_TIDY NAMES clang-tidy REQUIRED)
endif()
function(laso_target target)
  set_target_properties(${target} PROPERTIES CXX_EXTENSIONS OFF)
  target_link_libraries(${target} PRIVATE laso_options)
  target_include_directories(${target} PUBLIC $<BUILD_INTERFACE:${PROJECT_SOURCE_DIR}/include>)
  if(LASO_ENABLE_TIDY)
    set_target_properties(${target} PROPERTIES CXX_CLANG_TIDY "${LASO_CLANG_TIDY}")
  endif()
endfunction()
add_library(laso_core src/core/types.cpp src/pipeline/parser.cpp src/core/config.cpp src/core/security.cpp src/core/artifacts.cpp src/providers/openai_compatible.cpp src/schema/validator.cpp src/workers/worker.cpp)
laso_target(laso_core)
target_link_libraries(laso_core PUBLIC nlohmann_json::nlohmann_json Boost::system Threads::Threads nlohmann_json_schema_validator PRIVATE ${LASO_YAML_TARGET} spdlog::spdlog)
target_compile_features(laso_core PUBLIC cxx_std_20)
add_library(laso_storage_sqlite src/storage/sqlite.cpp)
laso_target(laso_storage_sqlite)
target_link_libraries(laso_storage_sqlite PUBLIC laso_core PRIVATE SQLite::SQLite3)
add_library(laso_storage src/storage/factory.cpp)
laso_target(laso_storage)
target_link_libraries(laso_storage PUBLIC laso_core PRIVATE laso_storage_sqlite)
if(LASO_ENABLE_POSTGRES)
  add_library(laso_storage_postgres src/storage/postgres.cpp src/storage/postgres_pool.cpp
                                      src/storage/coordination.cpp)
  laso_target(laso_storage_postgres)
  target_link_libraries(laso_storage_postgres PUBLIC laso_core PRIVATE PkgConfig::LIBPQXX PkgConfig::LIBPQ)
  target_compile_definitions(laso_storage PRIVATE LASO_HAS_POSTGRES=1)
  target_link_libraries(laso_storage PRIVATE laso_storage_postgres)
endif()
add_library(laso_plugin_loader src/plugins/loader.cpp)
laso_target(laso_plugin_loader)
target_include_directories(laso_plugin_loader PUBLIC plugin_sdk/include)
target_link_libraries(laso_plugin_loader PUBLIC laso_core PRIVATE ${CMAKE_DL_LIBS})
add_library(laso_runtime src/runtime/executor.cpp src/nodes/nodes.cpp src/runtime/runtime.cpp src/runtime/graph.cpp src/runtime/execution.cpp src/scheduler/scheduler.cpp)
laso_target(laso_runtime)
target_link_libraries(laso_runtime PUBLIC laso_core laso_storage)
add_library(laso_application src/application/service.cpp src/application/event_ingress.cpp src/workers/manager.cpp src/workers/process_transport.cpp)
laso_target(laso_application)
target_link_libraries(laso_application PUBLIC laso_runtime laso_storage laso_plugin_loader)
add_library(laso_api src/api/api.cpp src/api/server.cpp)
laso_target(laso_api)
target_link_libraries(laso_api PUBLIC laso_application)
add_library(laso_cli src/cli/cli.cpp)
laso_target(laso_cli)
target_link_libraries(laso_cli PUBLIC laso_application PRIVATE CLI11::CLI11)
add_executable(laso apps/laso/main.cpp)
laso_target(laso)
target_link_libraries(laso PRIVATE laso_cli)
add_executable(laso-server apps/laso-server/main.cpp)
laso_target(laso-server)
target_link_libraries(laso-server PRIVATE laso_api CLI11::CLI11)
add_executable(laso-example-worker-host examples/process-worker/worker_host.cpp)
laso_target(laso-example-worker-host)
target_link_libraries(laso-example-worker-host PRIVATE laso_core)
if(LASO_BUILD_OPENCODE_ADAPTER)
  add_executable(laso-opencode-worker examples/opencode-worker/worker_host.cpp)
  laso_target(laso-opencode-worker)
  target_link_libraries(laso-opencode-worker PRIVATE laso_core)
  set_target_properties(laso-opencode-worker PROPERTIES RUNTIME_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/bin")
endif()
if(LASO_BUILD_CODEX_ADAPTER)
  add_executable(laso-codex-worker examples/codex-worker/worker_host.cpp)
  laso_target(laso-codex-worker)
  target_link_libraries(laso-codex-worker PRIVATE laso_core)
  set_target_properties(laso-codex-worker PROPERTIES RUNTIME_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/bin")
endif()
if(LASO_BUILD_CLAUDE_ADAPTER)
  add_executable(laso-claude-worker examples/claude-worker/worker_host.cpp)
  laso_target(laso-claude-worker)
  target_link_libraries(laso-claude-worker PRIVATE laso_core)
  set_target_properties(laso-claude-worker PROPERTIES RUNTIME_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/bin")
endif()
set_target_properties(laso laso-server laso-example-worker-host PROPERTIES RUNTIME_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/bin")
add_library(laso_example_tool MODULE plugin_sdk/examples/echo.c)
target_include_directories(laso_example_tool PRIVATE plugin_sdk/include)
set_target_properties(laso_example_tool PROPERTIES LIBRARY_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/plugins")
target_compile_options(laso_example_tool PRIVATE -Wall -Wextra -Wpedantic)
target_compile_features(laso_example_tool PRIVATE c_std_11)
target_compile_options(laso_example_tool PRIVATE ${LASO_SANITIZER_FLAGS})
target_link_options(laso_example_tool PRIVATE ${LASO_SANITIZER_FLAGS})
add_library(laso_example_model_provider MODULE plugin_sdk/examples/model_provider.c)
target_include_directories(laso_example_model_provider PRIVATE plugin_sdk/include)
target_compile_features(laso_example_model_provider PRIVATE c_std_11)
target_compile_options(laso_example_model_provider PRIVATE -Wall -Wextra -Wpedantic ${LASO_SANITIZER_FLAGS})
target_link_options(laso_example_model_provider PRIVATE ${LASO_SANITIZER_FLAGS})
set_target_properties(laso_example_model_provider PROPERTIES LIBRARY_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/plugins")
add_library(laso_example_event_source MODULE plugin_sdk/examples/event_source.c)
target_include_directories(laso_example_event_source PRIVATE plugin_sdk/include)
target_compile_features(laso_example_event_source PRIVATE c_std_11)
target_compile_options(laso_example_event_source PRIVATE -Wall -Wextra -Wpedantic ${LASO_SANITIZER_FLAGS})
target_link_options(laso_example_event_source PRIVATE ${LASO_SANITIZER_FLAGS})
set_target_properties(laso_example_event_source PROPERTIES LIBRARY_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/plugins")
add_library(laso_example_worker MODULE plugin_sdk/examples/worker.c)
target_include_directories(laso_example_worker PRIVATE plugin_sdk/include)
target_compile_features(laso_example_worker PRIVATE c_std_11)
target_compile_options(laso_example_worker PRIVATE -Wall -Wextra -Wpedantic ${LASO_SANITIZER_FLAGS})
target_link_options(laso_example_worker PRIVATE ${LASO_SANITIZER_FLAGS})
set_target_properties(laso_example_worker PROPERTIES LIBRARY_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/worker-plugins")
install(TARGETS laso laso-server RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR})
install(DIRECTORY include/ DESTINATION ${CMAKE_INSTALL_INCLUDEDIR})
install(FILES plugin_sdk/include/laso_plugin.h DESTINATION ${CMAKE_INSTALL_INCLUDEDIR})
install(FILES config/laso.example.yaml DESTINATION ${CMAKE_INSTALL_DATADIR}/laso)
if(BUILD_TESTING)
  find_package(GTest REQUIRED)
  add_library(laso_bad_abi MODULE tests/fixtures/bad_abi.c)
  target_include_directories(laso_bad_abi PRIVATE plugin_sdk/include)
  target_compile_features(laso_bad_abi PRIVATE c_std_11)
  target_compile_options(laso_bad_abi PRIVATE ${LASO_SANITIZER_FLAGS})
  target_link_options(laso_bad_abi PRIVATE ${LASO_SANITIZER_FLAGS})
  set_target_properties(laso_bad_abi PROPERTIES LIBRARY_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/bad-plugins")
  add_library(laso_event_source_lifecycle_fail MODULE tests/fixtures/event_source_lifecycle_fail.c)
  target_include_directories(laso_event_source_lifecycle_fail PRIVATE plugin_sdk/include)
  target_compile_features(laso_event_source_lifecycle_fail PRIVATE c_std_11)
  target_compile_options(laso_event_source_lifecycle_fail PRIVATE -Wall -Wextra -Wpedantic)
  set_target_properties(laso_event_source_lifecycle_fail PROPERTIES LIBRARY_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/event-bad-plugins")
  set(LASO_TEST_SOURCES
      tests/unit/domain.cpp
      tests/unit/schema.cpp
      tests/integration/runtime.cpp
      tests/integration/adapters.cpp
      tests/integration/scheduler.cpp
      tests/integration/event_sources.cpp
      tests/integration/workers.cpp
      tests/integration/process_workers.cpp)
  if(LASO_ENABLE_POSTGRES)
    list(APPEND LASO_TEST_SOURCES tests/integration/coordination.cpp)
  endif()
  if(LASO_BUILD_OPENCODE_ADAPTER)
    list(APPEND LASO_TEST_SOURCES tests/integration/opencode_worker.cpp)
  endif()
  if(LASO_BUILD_CODEX_ADAPTER)
    list(APPEND LASO_TEST_SOURCES tests/integration/codex_worker.cpp)
    add_executable(laso-codex-fixture tests/fixtures/codex_server.cpp)
    laso_target(laso-codex-fixture)
    target_link_libraries(laso-codex-fixture PRIVATE laso_core)
    set_target_properties(laso-codex-fixture PROPERTIES RUNTIME_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/bin")
  endif()
  if(LASO_BUILD_CLAUDE_ADAPTER)
    list(APPEND LASO_TEST_SOURCES tests/integration/claude_worker.cpp)
    add_executable(laso-claude-fixture tests/fixtures/claude_server.cpp)
    laso_target(laso-claude-fixture)
    target_link_libraries(laso-claude-fixture PRIVATE laso_core)
    set_target_properties(laso-claude-fixture PROPERTIES RUNTIME_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/bin")
  endif()
  add_executable(laso_tests ${LASO_TEST_SOURCES})
  laso_target(laso_tests)
  target_link_libraries(laso_tests PRIVATE laso_api GTest::gtest_main)
  target_compile_definitions(laso_tests PRIVATE LASO_SOURCE_DIR="${PROJECT_SOURCE_DIR}" LASO_PLUGIN_DIR="$<TARGET_FILE_DIR:laso_example_tool>" LASO_WORKER_PLUGIN_DIR="$<TARGET_FILE_DIR:laso_example_worker>" LASO_BAD_PLUGIN_DIR="$<TARGET_FILE_DIR:laso_bad_abi>" LASO_EVENT_PLUGIN_DIR="$<TARGET_FILE_DIR:laso_example_event_source>" LASO_EVENT_BAD_PLUGIN_DIR="$<TARGET_FILE_DIR:laso_event_source_lifecycle_fail>" LASO_PROCESS_WORKER_HOST="$<TARGET_FILE:laso-example-worker-host>")
  if(LASO_BUILD_OPENCODE_ADAPTER)
    target_compile_definitions(laso_tests PRIVATE LASO_OPENCODE_WORKER="$<TARGET_FILE:laso-opencode-worker>")
  endif()
  if(LASO_BUILD_CODEX_ADAPTER)
    target_compile_definitions(laso_tests PRIVATE LASO_CODEX_WORKER="$<TARGET_FILE:laso-codex-worker>" LASO_CODEX_FIXTURE="$<TARGET_FILE:laso-codex-fixture>")
  endif()
  if(LASO_BUILD_CLAUDE_ADAPTER)
    target_compile_definitions(laso_tests PRIVATE LASO_CLAUDE_WORKER="$<TARGET_FILE:laso-claude-worker>" LASO_CLAUDE_FIXTURE="$<TARGET_FILE:laso-claude-fixture>")
  endif()
  if(LASO_ENABLE_POSTGRES)
    target_compile_definitions(laso_tests PRIVATE LASO_HAS_POSTGRES=1)
    target_link_libraries(laso_tests PRIVATE PkgConfig::LIBPQXX PkgConfig::LIBPQ)
  endif()
  add_dependencies(laso_tests laso-example-worker-host laso_example_tool laso_example_model_provider laso_example_event_source laso_example_worker laso_bad_abi laso_event_source_lifecycle_fail)
  if(LASO_BUILD_OPENCODE_ADAPTER)
    add_dependencies(laso_tests laso-opencode-worker)
  endif()
  if(LASO_BUILD_CODEX_ADAPTER)
    add_dependencies(laso_tests laso-codex-worker laso-codex-fixture)
  endif()
  if(LASO_BUILD_CLAUDE_ADAPTER)
    add_dependencies(laso_tests laso-claude-worker laso-claude-fixture)
  endif()
  include(GoogleTest)
  gtest_discover_tests(laso_tests DISCOVERY_TIMEOUT 60)
  add_test(NAME cli_validate COMMAND laso pipeline validate ${PROJECT_SOURCE_DIR}/examples/hello-pipeline/pipeline.yaml)
  add_test(NAME process_smoke COMMAND bash ${PROJECT_SOURCE_DIR}/tests/integration/process-smoke.sh ${CMAKE_BINARY_DIR} ${PROJECT_SOURCE_DIR})
  set_tests_properties(process_smoke PROPERTIES TIMEOUT 60)
endif()
